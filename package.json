{
  "name": "SimpleSim",
  "version": "0.0.0",
  "description": "Display a timer that is in sync with any other user currently viewing the page",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dave Gasparini",
  "license": "ISC",
  "dependencies": {
    "ejs": "^1.0.0",
    "express": "^4.7.4",
    "mongoose": "3.8.3",
    "socket.io": "^1.0.6"
  }
}
